import React from 'react';
import ReactDOM from 'react-dom/client';
import StarryNight from './pages/home';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <StarryNight />
  </React.StrictMode>
);